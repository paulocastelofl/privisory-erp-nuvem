import { DataTablesSettingsDirective } from './data-tables-settings.directive';

describe('DataTablesSettingsDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTablesSettingsDirective();
    expect(directive).toBeTruthy();
  });
});
